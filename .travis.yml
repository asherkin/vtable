sudo: required
dist: trusty
language: generic
install:
  - sudo apt-get -qq update
  - sudo apt-get install -y emscripten
script:
  - make
  - make test
deploy:
  skip_cleanup: true
  provider: script
  script: make deploy
  on:
    branch: master
